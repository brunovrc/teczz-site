<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <defs>
    <filter id="sh" x="-20%" y="-20%" width="140%" height="140%">
      <feDropShadow dx="0" dy="0" stdDeviation="3" flood-color="#000" flood-opacity="0.45"/>
    </filter>
  </defs>
  <!-- T — barra horizontal -->
  <rect x="2" y="3" width="78" height="19" rx="3" fill="white" filter="url(#sh)"/>
  <!-- T — haste vertical centralizada -->
  <rect x="30" y="3" width="22" height="90" rx="3" fill="white" filter="url(#sh)"/>
  <!-- Ponto azul -->
  <circle cx="86" cy="87" r="13" fill="#3b82f6"/>
</svg>
